// Define one results js function which retrieves data from a container and
// renders it as HTML.

export function retrieveResults(element, t = 1) {
    // for(let i = 0; i !== t; i++) {
    //     const text = lines
    //         .slice(0, i+1)
    //         .join("\n");
    //
    //     element.innerHTML = `<pre>${text}</pre>`;
    // }
    //
    // // draw the next line unless we're done
    // if(t < lines.length) {
    //     setTimeout(() => makeAsciiArt(element, t + 1), 100);
    // }
}
